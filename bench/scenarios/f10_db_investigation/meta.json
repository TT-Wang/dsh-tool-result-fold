{"name": "f10_db_investigation", "turns": 2, "max_steps_per_turn": 60, "stressor": "a SQLite database (orders, customers, refunds, events with JSON payloads; ~25K rows) only reachable through db_query, which returns whole result sets; the investigation needs both aggregates and the content of event payloads for the suspicious customers"}
