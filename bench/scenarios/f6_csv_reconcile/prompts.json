["Two systems export the same warehouse inventory: exports/wms.csv and exports/erp.csv (columns: sku,category,warehouse,qty,unit_cost). Reconcile them: (1) write totals.json mapping each category to the total qty according to wms.csv; (2) write mismatches.csv with header sku,wms_qty,erp_qty,delta listing every sku whose qty differs between the two files (delta = wms_qty - erp_qty), sorted by sku; skus present in only one file count as qty 0 in the other. Reply DONE with the number of mismatches.",
 "Which single sku has the largest absolute delta, and what is the total absolute delta across all mismatches? Write worst.json as {\"sku\": ..., \"abs_delta\": N, \"total_abs_delta\": M}."]
