["We are integrating the Meridian SDK. Its documentation is at https://docs.example.com/ (start at the index page; use fetch_page — there is no local copy). Answer these from the docs and write answers.json with exactly these keys: {\"connect_timeout_default\": <number of seconds>, \"max_batch_size\": <number>, \"retry_backoff_default_ms\": <number>, \"deprecated_in\": <version string in which `Client.sync_all` was deprecated>, \"required_fields_create_order\": [<sorted list of required request fields of the create-order endpoint>], \"rate_limit_per_minute\": <number>, \"webhook_signature_header\": <header name>, \"error_code_insufficient_funds\": <the numeric code>}. Reply DONE when written.",
 "One more: the migration guide describes a behaviour change in how the client handles a lost connection mid-batch. In one sentence, what does the new version do, and which page did you find it on? Write it to migration.txt as `<sentence> | <url>`."]
