["An incident: checkout started failing around 14:32 today. The service logs for that window are under logs/ (one file per service). Investigate and write triage.md containing a markdown table with exactly these columns: service | code | count | first_seen — one row for every distinct ERROR code that appears in each service's log (count = number of ERROR lines with that code in that service; first_seen = the timestamp of the first such line, copied exactly as it appears in the log). Sort rows by service then code. Below the table write one line `root_cause: <service>/<code>` naming the service and error code you consider the origin of the cascade. Do it in one go and reply DONE when triage.md is written.",
 "Follow-up: in db-proxy's log, list the WARN lines that appear in the 60 seconds before its first ERROR line. Write them verbatim (whole lines, in order) into warns.txt, one per line, nothing else.",
 "Last one: write summary.md — a single paragraph (3–5 sentences) for the incident channel that names the root-cause service and error code, the first timestamp it appeared, and how many services were affected in total (services with at least one ERROR line)."]
