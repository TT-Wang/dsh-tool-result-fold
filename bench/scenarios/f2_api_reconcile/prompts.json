["Finance needs a reconciliation. Orders and refunds are only available through the local CLI documented in docs/API.md (python3 api.py ...). Find every order whose status is \"failed\", whose amount is at least 500, and that has NO refund referencing it. Write reconcile.json with exactly {\"order_ids\": [...sorted ascending...], \"total_amount\": <sum of their amounts, as a number>}. Work through all pages; do not stop early. Reply DONE with the count when the file is written.",
 "Also write stats.json with {\"orders_total\": <number of orders across all pages>, \"max_amount\": <largest order amount>, \"max_order_id\": <its id>, \"refunds_total\": <number of refunds>}."]
