{"name": "f8_verbose_tests", "turns": 2, "max_steps_per_turn": 60, "stressor": "a 500-test suite configured verbose by default (-vv --tb=long) with 12 planted bugs whose failures print long string diffs; each run is tens of thousands of chars; fix to green, then list the initially failing tests"}
