{"name": "f7_build_fix", "turns": 2, "max_steps_per_turn": 60, "stressor": "a build script over 60 modules prints ~4000 lines per run (progress and warnings noise) and fails on 6 planted errors in different modules; the fix loop is run→read→edit→run, and every run's output lands in context whatever the agent does with it"}
